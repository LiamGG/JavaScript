// FreeCodeCamp - https://www.freecodecamp.org/challenges/slasher-flick

// Return the remaining elements of the array, after chopping the number provided off the head

function slasher(arr, howMany) {

  // Return string after the amount chopped off.
  return arr.slice(howMany);

}
